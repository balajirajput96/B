<!DOCTYPE html>
# Balaji Digital Marketing

A responsive static marketing site is now available in `index.html` with its matching styles in `styles.css`.

Open `index.html` in any modern browser to preview the website. The contact form is intentionally presentation-only until a server-side endpoint is configured.
